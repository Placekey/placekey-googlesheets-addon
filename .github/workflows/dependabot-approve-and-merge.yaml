name: Dependabot approve and merge

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions: {}

jobs:
  dependabot-approve-and-merge:
    permissions:
      contents: write
      pull-requests: write
    secrets:
      CODEOWNER_PR_RW_TOKEN: ${{ secrets.PLACEKEY_GITHUB_CODEOWNER_PR_RW_TOKEN }}
    uses: senzing-factory/build-resources/.github/workflows/dependabot-approve-and-merge.yaml@a164168badaaadd037d3f0bf1dddf963dda5208a # v4
